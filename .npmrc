# npm 10's dependency resolver crashes on this tree ("Cannot read properties of
# null (reading 'edgesOut')") unless peer resolution is relaxed. Pinning it
# here means a clean install works the same way everywhere — CI, Vercel,
# Netlify, or somebody's laptop — instead of only where the flag was
# remembered on the command line.
legacy-peer-deps=true
