node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
*.tsbuildinfo

# Real customer exports used as local test fixtures. Never committed:
# sharing a counterparty's ledger is the account owner's decision, not the
# repository's.
sample-data/*.xlsx
sample-data/*.XLSX
